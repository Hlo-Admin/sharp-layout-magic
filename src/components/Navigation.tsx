// Navigation component is now integrated into the Hero component
// This file can be removed or kept for future reference

const Navigation = () => {
  return null; // Navigation is now handled within the Hero component
};

export default Navigation;
